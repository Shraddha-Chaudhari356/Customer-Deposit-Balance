<!--Correct and Updated Multi-Currency Code -->
<?xml version="1.0"?>
<!DOCTYPE pdf PUBLIC "-//big.faceless.org//report" "report-1.1.dtd">
<#if statements??>
  <#if statements?has_content>
    <#assign record = statements?first>
<#else>
</#if>

<pdf>
<head>
<link name="NotoSans" type="font" subtype="truetype" src="${nsfont.NotoSans_Regular}" src-bold="${nsfont.NotoSans_Bold}" src-italic="${nsfont.NotoSans_Italic}" src-bolditalic="${nsfont.NotoSans_BoldItalic}" bytes="2" />
	<#if .locale == "zh_CN">
		<link name="NotoSansCJKsc" type="font" subtype="opentype" src="${nsfont.NotoSansCJKsc_Regular}" src-bold="${nsfont.NotoSansCJKsc_Bold}" bytes="2" />
	<#elseif .locale == "zh_TW">
		<link name="NotoSansCJKtc" type="font" subtype="opentype" src="${nsfont.NotoSansCJKtc_Regular}" src-bold="${nsfont.NotoSansCJKtc_Bold}" bytes="2" />
	<#elseif .locale == "ja_JP">
		<link name="NotoSansCJKjp" type="font" subtype="opentype" src="${nsfont.NotoSansCJKjp_Regular}" src-bold="${nsfont.NotoSansCJKjp_Bold}" bytes="2" />
	<#elseif .locale == "ko_KR">
		<link name="NotoSansCJKkr" type="font" subtype="opentype" src="${nsfont.NotoSansCJKkr_Regular}" src-bold="${nsfont.NotoSansCJKkr_Bold}" bytes="2" />
	<#elseif .locale == "th_TH">
		<link name="NotoSansThai" type="font" subtype="opentype" src="${nsfont.NotoSansThai_Regular}" src-bold="${nsfont.NotoSansThai_Bold}" bytes="2" />
	</#if>
      <macrolist>
        <macro id="nlheader">
              <table class="header" style="width: 100%;">
   <tr>
    <!-- LEFT SIDE -->
    
    <td style="width: 60%; vertical-align: top;">
        <#if companyInformation.logoUrl?has_content>
            <img src="${companyInformation.logoUrl}" style="height:60px;" />
        </#if><br/>
        <b>${companyInformation.companyName}</b><br/>
        ${companyInformation.addressText}
    </td>
 <!-- RIGHT SIDE -->
    <td align="right" style="width: 40%; vertical-align: top;">
        <span class="title">${record@title!""}</span>
    </td>
</tr>
</table>
 <br/>
</macro>
        <macro id="nlfooter">
            <table class="footer" style="width: 100%;">
                <tr>
                    <td align="right"><pagenumber/> of <totalpages/></td>
                </tr>
            </table>
        </macro>
    </macrolist>

<style type="text/css">
		* {
			<#if .locale == "zh_CN">
				font-family: NotoSans, NotoSansCJKsc, sans-serif;
			<#elseif .locale == "zh_TW">
				font-family: NotoSans, NotoSansCJKtc, sans-serif;
			<#elseif .locale == "ja_JP">
				font-family: NotoSans, NotoSansCJKjp, sans-serif;
			<#elseif .locale == "ko_KR">
				font-family: NotoSans, NotoSansCJKkr, sans-serif;
			<#elseif .locale == "th_TH">
				font-family: NotoSans, NotoSansThai, sans-serif;
			<#else>
				font-family: NotoSans, sans-serif;
			</#if>
		}
		table {
			font-size: 9pt;
			table-layout: fixed;
		}
        th {
            font-weight: bold;
            font-size: 8pt;
            vertical-align: middle;
            padding: 5px 6px 3px;
            background-color: #e3e3e3;
            color: #333333;
        }
        td {
            padding: 1px 4px;
        }
		td p { align:left }
        b {
            font-weight: bold;
            color: #333333;
        }
        table.header td {
            padding: 0;
            font-size: 10pt;
        }
        table.footer td {
            padding: 0;
            font-size: 8pt;
        }
        table.itemtable th {
            padding-bottom: 10px;
            padding-top: 10px;
        }
        table.body td {
            padding-top: 2px;
        }
        td.addressheader {
            font-size: 8pt;
            padding-top: 6px;
            padding-bottom: 2px;
        }
        td.address {
            padding-top: 0px;
        }
        span.title {
            font-size: 28pt;
        }
        span.number {
            font-size: 16pt;
        }
        hr {
            border-top: 1px dashed #d3d3d3;
            width: 100%;
            color: #ffffff;
            background-color: #ffffff;
            height: 1px;
        }
    </style>
</head>

<body header="nlheader" header-height="9%" footer="nlfooter" footer-height="20pt" padding="0.5in 0.5in 0.5in 0.5in" size="Letter">

<!-- Deposit JSON -->
<#assign depositJson = customer.custpage_deposit_json_view!"">
<#assign depositData = {}>

<#if depositJson?has_content>
    <#attempt>
        <#assign depositData = depositJson?eval>
    <#recover>
        <#assign depositData = {}>
    </#attempt>
</#if>

<!-- LOOP  -->
<#list statements as record>

<#assign currency = record.currency!"" >
<#assign deposit = depositData[currency]!{} >
<#assign remaining = deposit.remaining!0 >

<!--  dynamic symbol (no hardcode dependency) -->
<#assign symbol = record.currencysymbol!currency >

<#if record_index != 0>
    <p style="page-break-before: always;"></p>
</#if>

<table align="right">
    <tr>
        <td><b>Date:</b></td>
        <td align="right">${record.trandate}</td>
    </tr>
    <tr>
        <td><b>Amount Due:</b></td>
        <td align="right">${record.amountDue?abs}</td>
    </tr>
   <tr>
        <td><b>Amount Encl.</b></td>
        <td align="right">${record.amountEncl}</td>
    </tr>
   <tr>
        <td><b>Currency:</b></td>
        <td align="right">${record.currency}</td>
    </tr>
   <tr>
        <td><b>Subsidiary :</b></td>
        <td align="right">${record.subsidiary}</td>
    </tr>
</table>

<!--STATEMENT ALWAYS PRINTS -->
<table style="width: 100%;">
    <tr>
        <td><b>${record.billaddress@label!""}</b></td>
    </tr>
    <tr>
        <td>${record.billaddress!""}</td>
    </tr>
</table>
  <br/>

<!--  DEPOSIT SECTION  -->
<table style="width:100%; margin-top:10px;">
    <tr>
        <th style="text-align:center;">Currency</th>
        <th style="text-align:right;">Deposit Balance</th>
    </tr>
    <tr>
        <td style="text-align:center;">${currency}</td>
        <td style="text-align:right;">
            ${symbol} ${remaining?string["#,##0.00"]}
        </td>
    </tr>
</table>

<br/>
<table class="itemtable" style="width: 100%; margin-top: 10px;">
        <thead>
        <tr>
            <th colspan="3">${record.lines.datecol@label}</th>
            <th colspan="12">${record.lines.description@label}</th>
            <th align="right" colspan="3">${record.lines.charge@label}</th>
            <th align="right" colspan="4">${record.lines.payment@label}</th>
            <th align="right" colspan="4">${record.lines.balance@label}</th>
        </tr>
    </thead>

<#list record.lines as line>
<tr>
    <td colspan="3">${line.datecol}</td>
     <td colspan="12">${line.description}</td>
    <td align="right" colspan="3">${line.charge}</td>
     <td align="right" colspan="4">${line.payment}</td>
    <td align="right" colspan="4">${line.balance}</td>
</tr>
</#list>

</table>

<br/>

<!--  AGING -->
<table style="width: 100%; margin-top: 10px;">
<tr>
    <th>${record.aging1@label}</th>
    <th>${record.aging2@label}</th>
    <th>${record.aging3@label}</th>
    <th>${record.aging4@label}</th>
    <th>${record.aging5@label}</th>
   <th>${record.agingbal@label}</th>
</tr>

<tr>
    <td>${record.aging1}</td>
    <td>${record.aging2}</td>
    <td>${record.aging3}</td>
    <td>${record.aging4}</td>
    <td>${record.aging5}</td>
   <td>${record.agingbal?abs}</td>
</tr>
</table>
  
		<#if preferences.RETURNFORM && remittanceSlip??>

        <hr />
        <div class="remittanceSlip">
            <table style="width: 100%; margin-top: 10px;">
                <tr>
                    <td><span class="nameandaddress">${companyInformation.companyName}</span></td>
                    <td align="right"><span class="number">${remittanceSlip@title}</span></td>
                </tr>
            </table>
            <table style="width: 100%; margin-top: 10px;">
                <tr>
                    <th>${remittanceSlip.customername@label}</th>
                    <th>${record.trandate@label}</th>
                    <th>${record.amountDue@label}</th>
                    <th>${remittanceSlip.amountPaid@label}</th>
                </tr>
                <tr>
                    <td>${companyInformation.addressText}</td>
                    <td>${record.trandate}</td>
                           <td align="right">${symbol} ${record.amountDue?abs}</td>
                    <td></td>
                </tr>
            </table>
            <table style="width: 100%; margin-top: 10px;">
                <tr>
                    <th>${remittanceSlip.ccinfo@label}</th>
                    <th>${remittanceSlip.companyaddress@label}</th>
                </tr>
                <tr>
                    <td>${remittanceSlip.ccinfo}</td>
                    <td>${companyInformation.addressText}</td>
                </tr>
            </table>
        </div>
           </#if>

</#list>

</body>
</pdf>

<#else>
<pdf><body>No data found</body></pdf>
</#if>